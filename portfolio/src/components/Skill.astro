---
const {
  skill: { name, image },
} = Astro.props;
---

<div
  class="bg-gray-800 shadow-xl rounded-md p-1 flex flex-col justify-evenly content-center items-center w-[75px]"
>
  <img src={"/public/" + image} alt={name} class="w-[40px] h-[40px]" />
  <p class="text-xs text-gray-200 font-bold">{name}</p>
</div>
